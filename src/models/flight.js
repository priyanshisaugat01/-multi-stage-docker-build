 
class Flight {
  constructor(flight, origin, destination, status) {
    this.flight = flight;
    this.origin = origin;
    this.destination = destination;
    this.status = status;
  }
}

module.exports = Flight;
